export type CustomerProfile = {
  name: string;
  phone: string;
  email: string;
  birthday: string;
  notes: string;
};

export type OrderItem = {
  id: string;
  name: string;
  photo: string;
  createdAt: string;
};
