export type CustomerProfile = {
  name: string;
  phone: string;
  email: string;
  birthday: string;
  note: string;
};

export type OrderItem = {
  id: string;
  name: string;
  photo: string;
  createdAt: string;
};
